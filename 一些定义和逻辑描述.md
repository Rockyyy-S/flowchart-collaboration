# 新需求

## 1. 团队功能

- 一个团队有多个成员组成。
- 在新建团队时可以添加成员。
- 也可以后续往团队中添加/删除成员。
- 同一个人可以在多个团队里面。
- 只有团队创建者才能删除团队、添加/删除团队成员。
- 团队功能服务于流程图，不是复杂的团队管理功能。

## 2. 项目功能

- 一个项目必须跟一个团队绑定，而且只能跟一个团队绑定。
- 只有项目创建者才能删除项目、绑定团队。
- 项目对应的团队成员可以查看自己参与的项目。
- 每个项目有一个项目空间，里面包含该项目的所有流程图。
- 项目功能服务于流程图，不是复杂的项目管理功能。

## 3. 流程图

- 一个流程图只属于一个项目。
- 一个流程图包含多个节点。
- 一个流程图必须包含开始节点和结束节点。
- 一个带有分支的流程图可以把分支拆出来，拆出后的分支流程图称为该流程图的子流程图。

## 4. 子流程图

- 子流程图具有普通流程图所有的特性。
- 流程图数据结构设计两个属性：父流程图、父节点。子流程图的父流程图和父节点属性必须不为空，为空就表示是单独的流程图。

## 5. 节点

- 一个节点只属于一个流程图。
- 节点类型有开始节点、结束节点和任务节点。
- 开始节点和结束节点不是任务节点，因为它们只表示当前这个流程图所对应的项目是否开始或是否完成的状态。
- 任务节点必须在开始节点和结束节点之间。
- 任务节点又分为有分支任务节点和无分支任务节点。
- 无分支任务节点一般表示该节点对应的项目环节比较简单，只需要一个人来完成，无需其他人协作。
- 有分支任务节点一般表示该节点对应的项目环节比较复杂，需要多人协作完成，并且都完成后才能继续往后面的环节推进。
- 开始节点和结束节点不能有分支。
- 节点上如果有分支，那么该节点的分支数量一定是大于等于 2。因为如果只有一个分支，那么该分支应该合并到当前流程图中。
- 开始节点、结束节点、有分支任务节点和无分支任务节点用贴合节点语义的不同的流程图线框表示，一眼就能看出是什么节点。
- 待定：有分支任务节点的线框图如何设计？要和无分支任务节点线框图基本一致。无分支任务节点线框图是个长方形，里面显示节点基本信息。有分支任务节点线框图也是长方形，但是显示两列，前面一列显示节点基本信息，后面一列显示子流程图列表。点击子流程图列表项可以展开在节点上/下方（如果展开多个，需要均匀分布在当前节点上下方），每个子流程图列表项后面提供跳转按钮，点击可以打开子流程图。
- 点击节点后在右侧弹出窗口，展示节点具体信息和一些操作入口。
- 参与者只能在右侧弹窗操作自己所在的那个节点。
- 非当前节点参与者点击当前节点不会弹出右侧弹窗。比如：一个节点中参与者不包含团队成员张三，那么张三只能通过流程图查看节点基本信息，不能通过点击节点弹出右侧弹窗。
- 一个节点要成功推进到下一个节点，需要下一个节点的所有参与者审核当前节点的产出物是否符合要求。如果不符合，下一个节点的参与者有权拒绝往下流转。
- 节点的右侧弹窗分为三部分。第一部分展示节点基本信息。第二部分展示上个节点的产物并提供审核功能，审核功能包含通过、拒绝按钮和输入拒绝理由的输入框，点击拒绝按钮后，流程会回退到上个节点。第三部分提供上传当前节点产物的入口。最后是完成按钮，点击完成后流程流转到下个节点。

## 6. 团队数据结构

- ID
- 团队名
- 团队描述
- 团队成员列表
- 创建时间
- 创建人

## 7. 项目数据结构

- ID
- 项目名
- 项目描述
- 创建时间
- 创建人
- 流程图列表
- 绑定的团队

## 8. 流程图数据结构

- ID
- 名称
- 描述
- 父流程图
- 父节点
- 所属项目
- 负责人
- 节点数量
- 状态（0，1，2，3 分别代表未开始，进行中，已完成，超过截止时间）
- 创建时间
- 开始时间
- 完成时间
- 截止时间

## 9. 节点数据结构

- ID
- 名称
- 描述
- 节点类型
- 状态（0，1，2，3，4 分别代表未开始，进行中，已完成，超过截止时间，被下个节点参与者回退）
- 所属流程图
- 负责人
- 创建时间
- 开始时间
- 完成时间
- 截止时间
- 子流程图列表

## 10. 其他事项

- ID需要有前缀，比如project-xxx、flowchart-xxx、node-xxx等。
- 删除项目会把该项目下的所有流程图一起删除。删除时需要给提示。
- 删除流程图会把该流程图及其子流程图数据全部删除。删除时需要给提示。
- 可以同时打开多个流程图，在画布区域最上方提供切换的TAP，每个TAP名按“项目名/流程名”这样的格式展示。
